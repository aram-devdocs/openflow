# OpenFlow Environment Variables
#
# Copy this file to .env or .env.local and modify as needed.
# See CLAUDE.md "Environment Variables" section for complete documentation.

# =============================================================================
# Frontend Environment Variables (VITE_ prefix)
# =============================================================================

# Backend server URL for browser mode (HTTP transport).
# Only used when NOT running in Tauri (e.g., `pnpm dev:web`).
# When running in Tauri (`pnpm dev`), the app uses IPC and ignores this.
#
# Default: http://localhost:3001
# Example for remote server: https://api.openflow.example.com
#
# VITE_BACKEND_URL=http://localhost:3001

# =============================================================================
# Backend Environment Variables (Server mode: `pnpm dev:server`)
# =============================================================================

# Host address for the HTTP server to bind to.
# Use 0.0.0.0 to listen on all interfaces (e.g., for Docker).
#
# Default: 127.0.0.1
#
# OPENFLOW_HOST=127.0.0.1

# Port for the HTTP server.
#
# Default: 3001
#
# OPENFLOW_PORT=3001

# Path to the SQLite database file.
# In Tauri mode, this is managed automatically in the app data directory.
# In server mode, you can set a custom path.
#
# Default: ./openflow.db
#
# DATABASE_PATH=./openflow.db

# Alternative: SQLite connection URL format.
# Takes precedence over DATABASE_PATH if set.
# Example: sqlite:/var/lib/openflow/data.db
#
# DATABASE_URL=

# Alternative: Directory for database files.
# The database file will be named "openflow.db" within this directory.
# Used if neither DATABASE_URL nor DATABASE_PATH is set.
#
# OPENFLOW_DATA_DIR=

# Maximum number of database connections in the pool.
# Increase for high-concurrency deployments.
#
# Default: 5
#
# DATABASE_MAX_CONNECTIONS=5

# Allowed CORS origins for the HTTP server (comma-separated).
# Required for browser clients connecting from different origins.
#
# Default: localhost:5173,localhost:1420
#
# OPENFLOW_CORS_ORIGINS=localhost:5173,localhost:1420

# Enable verbose logging.
# Set to 'true' for detailed debug output.
#
# Default: false
#
# OPENFLOW_VERBOSE=false

# Rust log level filter.
# Controls which log messages are displayed.
# Options: error, warn, info, debug, trace
# Can also be set per-module, e.g., openflow_server=debug,sqlx=warn
#
# Default: info
#
# RUST_LOG=info

# Enable JSON log format for log aggregation.
# Set to '1' for JSON output (useful in Docker/production).
#
# Default: 0 (human-readable logs)
#
# OPENFLOW_JSON_LOGS=0

# =============================================================================
# Docker Environment Variables
# =============================================================================
# These variables are used by docker-compose.yml.
# The OPENFLOW_* variables above are also used inside the container.

# Host port to expose the server on.
# Maps to container port 3001.
#
# Default: 3001
#
# OPENFLOW_PORT=3001
