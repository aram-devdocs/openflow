// =============================================================================
// AUTO-GENERATED FILE - DO NOT EDIT MANUALLY
// =============================================================================
//
// Generated by: scripts/generate-queries.ts
// Source: crates/openflow-contracts/src/endpoints/mod.rs
// Domain: agents
// Generated at: 2026-01-06T04:36:16.754Z
//
// This file contains type-safe query functions for the agents domain.
// Each function wraps a Tauri IPC command with validation and logging.
//
// To regenerate: pnpm generate:queries
//
// @see CLAUDE.md - Query Layer Patterns section
// =============================================================================

import type {
  AgentEventRecord,
  AgentSession,
  AgentSessionWithState,
  Permission,
  ResizeAgentSessionRequest,
  RespondPermissionRequest,
  SessionSummary,
  WriteAgentInputRequest,
} from '@openflow/generated';
import { createLogger } from '@openflow/utils';
import {
  resizeAgentSessionRequestSchema,
  respondPermissionRequestSchema,
  writeAgentInputRequestSchema,
} from '@openflow/validation';
import { invoke } from '../utils.js';

const logger = createLogger('queries:agents:generated');

/**
 * Get pending permission request for an agent session
 *
 * @endpoint GET /api/agents/sessions/:id/permission
 * @command get_agent_pending_permission
 * @param id - Path parameter: id
 * @returns Promise resolving to Permission
 * @throws Error if validation or query fails
 */
export async function getAgentPendingPermission(id: string): Promise<Permission> {
  logger.debug('Calling get_agent_pending_permission', { id });

  try {
    const result = await invoke<Permission>('get_agent_pending_permission', { id: id });

    logger.info('get_agent_pending_permission completed', {
      id: (result as unknown as Record<string, unknown>).id,
    });

    return result;
  } catch (error) {
    const errorMessage = error instanceof Error ? error.message : String(error);
    logger.error('get_agent_pending_permission failed', { error: errorMessage });
    throw error;
  }
}

/**
 * Get raw output from an agent session
 *
 * @endpoint GET /api/agents/sessions/:id/raw-output
 * @command get_agent_raw_output
 * @param id - Path parameter: id
 * @returns Promise resolving to string
 * @throws Error if validation or query fails
 */
export async function getAgentRawOutput(id: string): Promise<string> {
  logger.debug('Calling get_agent_raw_output', { id });

  try {
    const result = await invoke<string>('get_agent_raw_output', { id: id });

    logger.info('get_agent_raw_output completed');

    return result;
  } catch (error) {
    const errorMessage = error instanceof Error ? error.message : String(error);
    logger.error('get_agent_raw_output failed', { error: errorMessage });
    throw error;
  }
}

/**
 * Get an agent session by ID
 *
 * @endpoint GET /api/agents/sessions/:id
 * @command get_agent_session
 * @param id - Path parameter: id
 * @returns Promise resolving to AgentSession
 * @throws Error if validation or query fails
 */
export async function getAgentSession(id: string): Promise<AgentSession> {
  logger.debug('Calling get_agent_session', { id });

  try {
    const result = await invoke<AgentSession>('get_agent_session', { id: id });

    logger.info('get_agent_session completed', {
      id: (result as unknown as Record<string, unknown>).id,
    });

    return result;
  } catch (error) {
    const errorMessage = error instanceof Error ? error.message : String(error);
    logger.error('get_agent_session failed', { error: errorMessage });
    throw error;
  }
}

/**
 * Get events for an agent session
 *
 * @endpoint GET /api/agents/sessions/:id/events
 * @command get_agent_session_events
 * @param id - Path parameter: id
 * @param afterSequence - Query parameter: afterSequence (optional)
 * @returns Promise resolving to AgentEventRecord[]
 * @throws Error if validation or query fails
 */
export async function getAgentSessionEvents(
  id: string,
  afterSequence?: string
): Promise<AgentEventRecord[]> {
  logger.debug('Calling get_agent_session_events', { id });

  try {
    const result = await invoke<AgentEventRecord[]>('get_agent_session_events', {
      id: id,
      afterSequence: afterSequence,
    });

    logger.info('get_agent_session_events completed', { count: result.length });

    return result;
  } catch (error) {
    const errorMessage = error instanceof Error ? error.message : String(error);
    logger.error('get_agent_session_events failed', { error: errorMessage });
    throw error;
  }
}

/**
 * Get a summary of an agent session
 *
 * @endpoint GET /api/agents/sessions/:id/summary
 * @command get_agent_session_summary
 * @param id - Path parameter: id
 * @returns Promise resolving to SessionSummary
 * @throws Error if validation or query fails
 */
export async function getAgentSessionSummary(id: string): Promise<SessionSummary> {
  logger.debug('Calling get_agent_session_summary', { id });

  try {
    const result = await invoke<SessionSummary>('get_agent_session_summary', { id: id });

    logger.info('get_agent_session_summary completed', {
      id: (result as unknown as Record<string, unknown>).id,
    });

    return result;
  } catch (error) {
    const errorMessage = error instanceof Error ? error.message : String(error);
    logger.error('get_agent_session_summary failed', { error: errorMessage });
    throw error;
  }
}

/**
 * Get an agent session with computed state
 *
 * @endpoint GET /api/agents/sessions/:id/with-state
 * @command get_agent_session_with_state
 * @param id - Path parameter: id
 * @returns Promise resolving to AgentSessionWithState
 * @throws Error if validation or query fails
 */
export async function getAgentSessionWithState(id: string): Promise<AgentSessionWithState> {
  logger.debug('Calling get_agent_session_with_state', { id });

  try {
    const result = await invoke<AgentSessionWithState>('get_agent_session_with_state', { id: id });

    logger.info('get_agent_session_with_state completed', {
      id: (result as unknown as Record<string, unknown>).id,
    });

    return result;
  } catch (error) {
    const errorMessage = error instanceof Error ? error.message : String(error);
    logger.error('get_agent_session_with_state failed', { error: errorMessage });
    throw error;
  }
}

/**
 * Kill an agent session
 *
 * @endpoint POST /api/agents/sessions/:id/kill
 * @command kill_agent_session
 * @param id - Path parameter: id
 * @returns Promise resolving to AgentSession
 * @throws Error if validation or query fails
 */
export async function killAgentSession(id: string): Promise<AgentSession> {
  logger.debug('Calling kill_agent_session', { id });

  try {
    const result = await invoke<AgentSession>('kill_agent_session', { id: id });

    logger.info('kill_agent_session completed', {
      id: (result as unknown as Record<string, unknown>).id,
    });

    return result;
  } catch (error) {
    const errorMessage = error instanceof Error ? error.message : String(error);
    logger.error('kill_agent_session failed', { error: errorMessage });
    throw error;
  }
}

/**
 * List agent sessions for a process
 *
 * @endpoint GET /api/agents/sessions/by-process/:processId
 * @command list_agent_sessions_by_process
 * @param processId - Path parameter: processId
 * @returns Promise resolving to AgentSession[]
 * @throws Error if validation or query fails
 */
export async function listAgentSessionsByProcess(processId: string): Promise<AgentSession[]> {
  logger.debug('Calling list_agent_sessions_by_process', { processId });

  try {
    const result = await invoke<AgentSession[]>('list_agent_sessions_by_process', {
      processId: processId,
    });

    logger.info('list_agent_sessions_by_process completed', { count: result.length });

    return result;
  } catch (error) {
    const errorMessage = error instanceof Error ? error.message : String(error);
    logger.error('list_agent_sessions_by_process failed', { error: errorMessage });
    throw error;
  }
}

/**
 * Recover stale agent sessions
 *
 * @endpoint POST /api/agents/sessions/recover
 * @command recover_stale_sessions
 * @returns Promise resolving to number
 * @throws Error if validation or query fails
 */
export async function recoverStaleSessions(): Promise<number> {
  logger.debug('Calling recover_stale_sessions');

  try {
    const result = await invoke<number>('recover_stale_sessions');

    logger.info('recover_stale_sessions completed');

    return result;
  } catch (error) {
    const errorMessage = error instanceof Error ? error.message : String(error);
    logger.error('recover_stale_sessions failed', { error: errorMessage });
    throw error;
  }
}

/**
 * Resize an agent session terminal
 *
 * @endpoint POST /api/agents/sessions/:id/resize
 * @command resize_agent_session
 * @param id - Path parameter: id
 * @param request - Request body (validated with Zod)
 * @returns Promise resolving to void
 * @throws Error if validation or query fails
 */
export async function resizeAgentSession(
  id: string,
  request: ResizeAgentSessionRequest
): Promise<void> {
  logger.debug('Calling resize_agent_session', { id });

  try {
    const validated = resizeAgentSessionRequestSchema.parse(request);
    await invoke<void>('resize_agent_session', { id: id, request: validated });

    logger.info('resize_agent_session completed successfully');
  } catch (error) {
    const errorMessage = error instanceof Error ? error.message : String(error);
    logger.error('resize_agent_session failed', { error: errorMessage });
    throw error;
  }
}

/**
 * Respond to an agent permission request
 *
 * @endpoint POST /api/agents/sessions/:id/permission/respond
 * @command respond_agent_permission
 * @param id - Path parameter: id
 * @param request - Request body (validated with Zod)
 * @returns Promise resolving to Permission
 * @throws Error if validation or query fails
 */
export async function respondAgentPermission(
  id: string,
  request: RespondPermissionRequest
): Promise<Permission> {
  logger.debug('Calling respond_agent_permission', { id });

  try {
    const validated = respondPermissionRequestSchema.parse(request);
    const result = await invoke<Permission>('respond_agent_permission', {
      id: id,
      request: validated,
    });

    logger.info('respond_agent_permission completed', {
      id: (result as unknown as Record<string, unknown>).id,
    });

    return result;
  } catch (error) {
    const errorMessage = error instanceof Error ? error.message : String(error);
    logger.error('respond_agent_permission failed', { error: errorMessage });
    throw error;
  }
}

/**
 * Write input to an agent session
 *
 * @endpoint POST /api/agents/sessions/:id/input
 * @command write_agent_input
 * @param id - Path parameter: id
 * @param request - Request body (validated with Zod)
 * @returns Promise resolving to void
 * @throws Error if validation or query fails
 */
export async function writeAgentInput(id: string, request: WriteAgentInputRequest): Promise<void> {
  logger.debug('Calling write_agent_input', { id });

  try {
    const validated = writeAgentInputRequestSchema.parse(request);
    await invoke<void>('write_agent_input', { id: id, request: validated });

    logger.info('write_agent_input completed successfully');
  } catch (error) {
    const errorMessage = error instanceof Error ? error.message : String(error);
    logger.error('write_agent_input failed', { error: errorMessage });
    throw error;
  }
}
