// =============================================================================
// AUTO-GENERATED FILE - DO NOT EDIT MANUALLY
// =============================================================================
//
// Generated by: scripts/generate-queries.ts
// Source: crates/openflow-contracts/src/endpoints/mod.rs
// Domain: executor
// Generated at: 2026-01-02T23:08:08.820Z
//
// This file contains type-safe query functions for the executor domain.
// Each function wraps a Tauri IPC command with validation and logging.
//
// To regenerate: pnpm generate:queries
//
// @see CLAUDE.md - Query Layer Patterns section
// =============================================================================

import type {
  CreateExecutorProfileRequest,
  ExecutionProcess,
  ExecutorProfile,
  RunExecutorRequest,
  UpdateExecutorProfileRequest,
} from '@openflow/generated';
import { createLogger } from '@openflow/utils';
import {
  createExecutorProfileRequestSchema,
  runExecutorRequestSchema,
  updateExecutorProfileRequestSchema,
} from '@openflow/validation';
import { invoke } from '../utils.js';

const logger = createLogger('queries:executor:generated');

/**
 * Create a new executor profile
 *
 * @endpoint POST /api/executor-profiles
 * @command create_executor_profile
 * @param request - Request body (validated with Zod)
 * @returns Promise resolving to ExecutorProfile
 * @throws Error if validation or query fails
 */
export async function createExecutorProfile(
  request: CreateExecutorProfileRequest
): Promise<ExecutorProfile> {
  logger.debug('Calling create_executor_profile');

  try {
    const validated = createExecutorProfileRequestSchema.parse(request);
    const result = await invoke<ExecutorProfile>('create_executor_profile', { request: validated });

    logger.info('create_executor_profile completed', {
      id: (result as unknown as Record<string, unknown>).id,
    });

    return result;
  } catch (error) {
    const errorMessage = error instanceof Error ? error.message : String(error);
    logger.error('create_executor_profile failed', { error: errorMessage });
    throw error;
  }
}

/**
 * Delete an executor profile
 *
 * @endpoint DELETE /api/executor-profiles/:id
 * @command delete_executor_profile
 * @param id - Path parameter: id
 * @returns Promise resolving to void
 * @throws Error if validation or query fails
 */
export async function deleteExecutorProfile(id: string): Promise<void> {
  logger.debug('Calling delete_executor_profile', { id });

  try {
    await invoke<void>('delete_executor_profile', { id: id });

    logger.info('delete_executor_profile completed successfully');
  } catch (error) {
    const errorMessage = error instanceof Error ? error.message : String(error);
    logger.error('delete_executor_profile failed', { error: errorMessage });
    throw error;
  }
}

/**
 * Get the default executor profile
 *
 * @endpoint GET /api/executor-profiles/default
 * @command get_default_executor_profile
 * @returns Promise resolving to ExecutorProfile
 * @throws Error if validation or query fails
 */
export async function getDefaultExecutorProfile(): Promise<ExecutorProfile> {
  logger.debug('Calling get_default_executor_profile');

  try {
    const result = await invoke<ExecutorProfile>('get_default_executor_profile');

    logger.info('get_default_executor_profile completed', {
      id: (result as unknown as Record<string, unknown>).id,
    });

    return result;
  } catch (error) {
    const errorMessage = error instanceof Error ? error.message : String(error);
    logger.error('get_default_executor_profile failed', { error: errorMessage });
    throw error;
  }
}

/**
 * Get an executor profile by ID
 *
 * @endpoint GET /api/executor-profiles/:id
 * @command get_executor_profile
 * @param id - Path parameter: id
 * @returns Promise resolving to ExecutorProfile
 * @throws Error if validation or query fails
 */
export async function getExecutorProfile(id: string): Promise<ExecutorProfile> {
  logger.debug('Calling get_executor_profile', { id });

  try {
    const result = await invoke<ExecutorProfile>('get_executor_profile', { id: id });

    logger.info('get_executor_profile completed', {
      id: (result as unknown as Record<string, unknown>).id,
    });

    return result;
  } catch (error) {
    const errorMessage = error instanceof Error ? error.message : String(error);
    logger.error('get_executor_profile failed', { error: errorMessage });
    throw error;
  }
}

/**
 * List all executor profiles
 *
 * @endpoint GET /api/executor-profiles
 * @command list_executor_profiles
 * @returns Promise resolving to ExecutorProfile[]
 * @throws Error if validation or query fails
 */
export async function listExecutorProfiles(): Promise<ExecutorProfile[]> {
  logger.debug('Calling list_executor_profiles');

  try {
    const result = await invoke<ExecutorProfile[]>('list_executor_profiles');

    logger.info('list_executor_profiles completed', { count: result.length });

    return result;
  } catch (error) {
    const errorMessage = error instanceof Error ? error.message : String(error);
    logger.error('list_executor_profiles failed', { error: errorMessage });
    throw error;
  }
}

/**
 * Run an AI executor in a chat
 *
 * @endpoint POST /api/executor/run
 * @command run_executor
 * @param request - Request body (validated with Zod)
 * @returns Promise resolving to ExecutionProcess
 * @throws Error if validation or query fails
 */
export async function runExecutor(request: RunExecutorRequest): Promise<ExecutionProcess> {
  logger.debug('Calling run_executor');

  try {
    const validated = runExecutorRequestSchema.parse(request);
    const result = await invoke<ExecutionProcess>('run_executor', { request: validated });

    logger.info('run_executor completed', {
      id: (result as unknown as Record<string, unknown>).id,
    });

    return result;
  } catch (error) {
    const errorMessage = error instanceof Error ? error.message : String(error);
    logger.error('run_executor failed', { error: errorMessage });
    throw error;
  }
}

/**
 * Update an executor profile
 *
 * @endpoint PATCH /api/executor-profiles/:id
 * @command update_executor_profile
 * @param id - Path parameter: id
 * @param request - Request body (validated with Zod)
 * @returns Promise resolving to ExecutorProfile
 * @throws Error if validation or query fails
 */
export async function updateExecutorProfile(
  id: string,
  request: UpdateExecutorProfileRequest
): Promise<ExecutorProfile> {
  logger.debug('Calling update_executor_profile', { id });

  try {
    const validated = updateExecutorProfileRequestSchema.parse(request);
    const result = await invoke<ExecutorProfile>('update_executor_profile', {
      id: id,
      request: validated,
    });

    logger.info('update_executor_profile completed', {
      id: (result as unknown as Record<string, unknown>).id,
    });

    return result;
  } catch (error) {
    const errorMessage = error instanceof Error ? error.message : String(error);
    logger.error('update_executor_profile failed', { error: errorMessage });
    throw error;
  }
}
