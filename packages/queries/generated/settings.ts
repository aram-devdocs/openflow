// =============================================================================
// AUTO-GENERATED FILE - DO NOT EDIT MANUALLY
// =============================================================================
//
// Generated by: scripts/generate-queries.ts
// Source: crates/openflow-contracts/src/endpoints/mod.rs
// Domain: settings
// Generated at: 2026-01-02T06:27:35.449Z
//
// This file contains type-safe query functions for the settings domain.
// Each function wraps a Tauri IPC command with validation and logging.
//
// To regenerate: pnpm generate:queries
//
// @see CLAUDE.md - Query Layer Patterns section
// =============================================================================

import type { Setting } from '@openflow/generated';
import { createLogger } from '@openflow/utils';
import { invoke } from '../utils.js';

const logger = createLogger('queries:settings:generated');

/**
 * Delete a setting
 *
 * @endpoint DELETE /api/settings/:key
 * @command delete_setting
 * @param key - Path parameter: key
 * @returns Promise resolving to void
 * @throws Error if validation or query fails
 */
export async function deleteSetting(key: string): Promise<void> {
  logger.debug('Calling delete_setting', { key });

  try {
    await invoke<void>('delete_setting', { key: key });

    logger.info('delete_setting completed successfully');
  } catch (error) {
    const errorMessage = error instanceof Error ? error.message : String(error);
    logger.error('delete_setting failed', { error: errorMessage });
    throw error;
  }
}

/**
 * Get all settings
 *
 * @endpoint GET /api/settings
 * @command get_all_settings
 * @returns Promise resolving to Setting[]
 * @throws Error if validation or query fails
 */
export async function getAllSettings(): Promise<Setting[]> {
  logger.debug('Calling get_all_settings');

  try {
    const result = await invoke<Setting[]>('get_all_settings');

    logger.info('get_all_settings completed', { count: result.length });

    return result;
  } catch (error) {
    const errorMessage = error instanceof Error ? error.message : String(error);
    logger.error('get_all_settings failed', { error: errorMessage });
    throw error;
  }
}

/**
 * Get a setting by key
 *
 * @endpoint GET /api/settings/:key
 * @command get_setting
 * @param key - Path parameter: key
 * @returns Promise resolving to Setting
 * @throws Error if validation or query fails
 */
export async function getSetting(key: string): Promise<Setting> {
  logger.debug('Calling get_setting', { key });

  try {
    const result = await invoke<Setting>('get_setting', { key: key });

    logger.info('get_setting completed', { id: (result as Record<string, unknown>).id });

    return result;
  } catch (error) {
    const errorMessage = error instanceof Error ? error.message : String(error);
    logger.error('get_setting failed', { error: errorMessage });
    throw error;
  }
}

/**
 * Get a setting value or return default
 *
 * @endpoint GET /api/settings/:key/default
 * @command get_setting_or_default
 * @param key - Path parameter: key
 * @param defaultValue - Query parameter: default_value (optional)
 * @returns Promise resolving to string
 * @throws Error if validation or query fails
 */
export async function getSettingOrDefault(key: string, defaultValue?: string): Promise<string> {
  logger.debug('Calling get_setting_or_default', { key });

  try {
    const result = await invoke<string>('get_setting_or_default', {
      key: key,
      default_value: defaultValue,
    });

    logger.info('get_setting_or_default completed');

    return result;
  } catch (error) {
    const errorMessage = error instanceof Error ? error.message : String(error);
    logger.error('get_setting_or_default failed', { error: errorMessage });
    throw error;
  }
}

/**
 * Set a setting value
 *
 * @endpoint PUT /api/settings/:key
 * @command set_setting
 * @param key - Path parameter: key
 * @param value - Query parameter: value (optional)
 * @returns Promise resolving to Setting
 * @throws Error if validation or query fails
 */
export async function setSetting(key: string, value?: string): Promise<Setting> {
  logger.debug('Calling set_setting', { key });

  try {
    const result = await invoke<Setting>('set_setting', { key: key, value: value });

    logger.info('set_setting completed', { id: (result as Record<string, unknown>).id });

    return result;
  } catch (error) {
    const errorMessage = error instanceof Error ? error.message : String(error);
    logger.error('set_setting failed', { error: errorMessage });
    throw error;
  }
}

/**
 * Check if a setting exists
 *
 * @endpoint GET /api/settings/:key/exists
 * @command setting_exists
 * @param key - Path parameter: key
 * @returns Promise resolving to boolean
 * @throws Error if validation or query fails
 */
export async function settingExists(key: string): Promise<boolean> {
  logger.debug('Calling setting_exists', { key });

  try {
    const result = await invoke<boolean>('setting_exists', { key: key });

    logger.info('setting_exists completed');

    return result;
  } catch (error) {
    const errorMessage = error instanceof Error ? error.message : String(error);
    logger.error('setting_exists failed', { error: errorMessage });
    throw error;
  }
}
